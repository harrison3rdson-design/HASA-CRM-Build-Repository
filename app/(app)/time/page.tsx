import {Panel} from "@/components/cards"; import {getTimeEntries} from "@/lib/data/app-data"; import {money,hours} from "@/lib/ui/format";
export default async function Page(){const rows:any[]=await getTimeEntries();return <><div className="page-heading"><div><h1>Time</h1><p>Live timer and manual time entry.</p></div><div className="button-row"><button className="secondary-button">Manual Entry</button><button className="primary-button">Start Timer</button></div></div><Panel title="Recent Time"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Project</th><th>Activity</th><th>Description</th><th>Hours</th><th>Value</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.work_date}</td><td>{x.project?.project_number??"—"}</td><td>{x.is_travel_time?"Travel Time":x.activity_type}</td><td>{x.description??"—"}</td><td>{hours(x.hours)}</td><td>{money(Number(x.hours)*Number(x.billing_rate))}</td></tr>)}</tbody></table></div></Panel></>}
