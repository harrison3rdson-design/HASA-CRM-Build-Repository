import {MetricCard,Panel} from "@/components/cards"; import {getInvoices} from "@/lib/data/app-data"; import {money} from "@/lib/ui/format";
export default async function Page(){const rows:any[]=await getInvoices();const ar=rows.reduce((s,x)=>s+Number(x.balance_due??0),0),paid=rows.reduce((s,x)=>s+Number(x.amount_paid??0),0);return <><div className="page-heading"><div><h1>Billing</h1><p>Advance, progress, hourly, expense, and final invoicing.</p></div><button className="primary-button">Create Invoice</button></div><div className="metric-grid compact"><MetricCard label="Outstanding" value={money(ar)}/><MetricCard label="Collected" value={money(paid)}/><MetricCard label="Invoices" value={rows.length}/></div><Panel title="Invoice Register"><div className="table-wrap"><table><thead><tr><th>Invoice</th><th>Client</th><th>Project</th><th>Type</th><th>Total</th><th>Balance</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.invoice_number}</td><td>{x.client?.company_name??"—"}</td><td>{x.project?.project_number??"—"}</td><td>{x.invoice_type}</td><td>{money(x.total)}</td><td>{money(x.balance_due)}</td><td><span className="pill">{x.status}</span></td></tr>)}</tbody></table></div></Panel></>}
