import {Panel} from "@/components/cards"; import {getReceiptInbox} from "@/lib/data/app-data";
export default async function Page(){const rows:any[]=await getReceiptInbox();return <><div className="page-heading"><div><h1>Receipt Inbox</h1><p>Capture first; assign and classify later.</p></div><button className="primary-button">Upload Receipt</button></div><Panel title="Inbox"><div className="table-wrap"><table><thead><tr><th>Captured</th><th>File</th><th>Type</th><th>Status</th><th>Assigned</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.captured_at).toLocaleString()}</td><td>{x.original_filename}</td><td>{x.mime_type??"—"}</td><td><span className="pill">{x.status}</span></td><td>{x.expense_id?"Expense":x.project_id?"Project":"Unassigned"}</td></tr>)}</tbody></table></div></Panel></>}
