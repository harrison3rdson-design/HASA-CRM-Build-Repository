import {Panel} from "@/components/cards"; import {getCompanySettings} from "@/lib/data/app-data";
export default async function Page(){const s:any=await getCompanySettings();return <><div className="page-heading"><div><h1>Settings</h1><p>Company, branding, billing defaults, and integrations.</p></div><button className="primary-button">Save Changes</button></div><div className="two-column"><Panel title="Company"><p><strong>{s.legal_name}</strong></p><p className="muted">Terms: {s.default_payment_terms} · Currency: {s.default_currency}</p></Panel><Panel title="Branding"><p>Horizontal logo: {s.logo_horizontal_path??"Not configured"}</p><p>Square logo: {s.logo_square_path??"Not configured"}</p></Panel><Panel title="Integrations"><p className="muted">SMS, email, storage, and PDF rendering are configured through server secrets.</p></Panel><Panel title="Security"><p className="muted">Permissions are enforced through authenticated server routes and PostgreSQL RLS.</p></Panel></div></>}
